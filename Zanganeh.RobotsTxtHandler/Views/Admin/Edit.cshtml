@model EditViewModel
@{  Layout = "Layout.cshtml"; }

@using (Html.BeginForm())
{
    @Html.DropDownList("SelectedSiteId", Model.Sites)
    @Html.TextAreaFor(a => a.RobotText)

    <input type="submit" value="Submit" />
}
